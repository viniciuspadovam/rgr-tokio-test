export interface ClientRequest {
    firstName: string;
    lastName: string;
    email: string;
}

export interface AddressRequest {
    address: string;
    number: string;
    complement: string;
    postalCode: string;
    city: string;
    state: string;
}
